export declare type OsmOAuth2Scopes = "read_prefs" | "write_prefs" | "write_diary" | "write_api" | "read_gpx" | "write_gpx" | "write_notes";
export declare type LoginOptions = {
    mode: "redirect" | "popup";
    redirectUrl: string;
    clientId: string;
    scopes: readonly OsmOAuth2Scopes[];
    popupSize?: readonly [width: number, height: number];
};
/** this is the payload that gets stored in localStorage */
export declare type LoginData = {
    /** ISO Date */
    issuedAt: string;
    accessToken: string;
    scopes: OsmOAuth2Scopes[];
};
